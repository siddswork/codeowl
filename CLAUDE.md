# CodeOwl — working conventions

CodeOwl extracts a structural graph from a codebase and serves LLM-authored specs (the semantic layer) over MCP. Design lives in `ARCHITECTURE.md` (how it's built) and `REQUIREMENTS.md` (what and for whom); `ROADMAP.md` has the build sequence and test repos; `GLOSSARY.md` defines the static-analysis / graph / CodeOwl-coined vocabulary the rest use (symbol, spec-bearing, flow edge, fan-in, the four hashes, "the socket held", …). Read those before proposing design changes — most "obvious" improvements have already been argued through and resolved there.

## Last Session (2026-09-25/26 — M21 (member-level extraction) shipped end to end: the interface_hash fold across all 4 packs, 3 rounds of `/code-review` (11 real findings fixed), a `file_is_spec_bearing` correction, real GitHub branch protection enabled on `master`, a full CodeOwl meetup demo built and successfully delivered, and M21.c's cross-stack test-gap closure. `master` is at `894bc2d`; one PR (#60) still open.)

**Goal:** finish M21 (which continuation-of-context started mid-flight — M21.a/M21.b's fold decision and first code-review round predate this transcript's visible start) and ship it cleanly, then close the test-coverage gaps two more `/code-review` rounds exposed. A large, unplanned detour in the middle: building and delivering a live conference demo.

**(a) M21.b's tail — two more `/code-review` rounds, then merged.** Round 2 (commit `ec523f1`) fixed 3 more real bugs on top of round 1's 3 (`30c807a`, predates this transcript): Python's signature-stripping prerequisite had broken `spec.rs`'s God-class reduction (a class attribute's default value silently dropped from what the LLM sees). Branch pushed, PR #58 opened and **merged**.

**(b) A design correction, found auditing `setup/` post-merge.** Asked to verify `setup/` docs were still accurate; instead found a real behavioral gap: `file_is_spec_bearing` required a file's top-level declaration to be *exported* to count toward spec coverage — but a barrel file is already excluded independently (it extracts to zero top-level symbols, no export check needed), so the export requirement's only real effect was making an **entirely-private file** (this repo's own `tests/lifecycle_matrix.rs` being the motivating case) permanently invisible to `get_spec_coverage`, forever, not merely deprioritized. Fixed (`file_is_spec_bearing` now checks only `kind`, never `is_exported`); `ARCHITECTURE.md`/`GLOSSARY.md`/`REQUIREMENTS.md` updated. **I committed this fix directly to `master` by mistake** — caught before pushing, moved to a proper branch, PR #59 opened and **merged**. In direct response, **enabled real GitHub branch protection on `master`** (`gh api .../branches/master/protection`: PR required, 1 approval, admin bypass allowed via `enforce_admins: false`, force-push and deletion both blocked) — previously "protected" was a `CLAUDE.md` convention only, not GitHub-enforced; it now is. A third `/code-review` round on the same branch (round 3) found one more real bug (Python's `interface_hash` silently truncating a multi-line public value to its first line) plus 4 cleanup items (a stale contradictory comment in `extract.rs`, a stale `REQUIREMENTS.md` line, `strip_value_for_fold` triplicated across 3 packs — consolidated into new `src/text.rs` — and `iface_rollup` computed unconditionally even for non-exported containers); all fixed and folded into PR #59 before merge.

**(c) An unplanned detour: a live CodeOwl demo, built and delivered.** The owner had a meetup talk the next day and asked for demo help. Grew into a full build: a new public repo, `https://github.com/siddswork/codeowl-meetup-demo` — a small Quarkus "Library Checkout" service (`Book` Panache entity, `CheckoutService`, `BookResource` 3 HTTP endpoints, `OverdueCheckJob` a `@Scheduled` job, `HealthResource` a deliberate bystander) with committed baseline specs and a `utility/` folder of pre-verified, narrated demo scripts (`00-before-you-start.sh` → live-generate a feature spec → `01`/`02` staleness-cascade tricks → `reset.sh`; an optional `99-add-style-guide.sh` showing a librarian-audience `STYLE.md`; `mcp_setup.sh` for VS Code + Copilot, auto-detecting the sibling `codeowl` repo). Along the way: installed Java 21 + Maven via SDKMAN on this machine (now permanent), verified the Java actually compiles (`mvn compile`, exit 0, in a disposable scratch copy first, then the real repo), fixed a real macOS incompatibility found live on the owner's Mac (`sed -i` differs GNU vs. BSD — switched to `perl -i -pe`, identical on both), and fixed three instances of a relative-path bug (scripts printed paths assuming the shell was at repo root, wrong when run from inside `utility/`, the owner's actual habit — fixed with absolute paths computed from each script's own location). **The owner confirmed: "the demo went well!!"** — this repo is done, not touched again after that; no further action needed unless raised again.

**(d) M21.c — all 4 planned items shipped, zero production code changes.** New branch `m21c-cross-stack-test-gaps`. `tests/lifecycle_matrix.rs` gained a 4th "unrelated" fixture file per stack (proven to never stale across any owner edit — item 1, `1e3da40`), an added/removed-public-field pair reusing one new fixture string in both directions (item 2+3, `e72190a`), and a method-signature-change scenario (item 4, `bca3f05`) — **item 4's own title turned out wrong once checked**: it assumed a method signature change cascades; verifying first (as the item's own text required) found every pack's fold is gated on `kind == SymbolKind::Value` with no exception, so a `Callable` is never folded in at all, signature or body — stays local, same as a body edit. Corrected in `ROADMAP.md` rather than silently written around. That finding produced **new `ARCHITECTURE.md` open question 13** (`3bb5886`): should a method's signature belong in `interface_hash` the way a field's now does? Deliberately left undecided — same shape open question 12 was before real measurement, not yet worth guessing at. Branch pushed, **PR #60 opened, still open** (no code-review round run on it — asked, owner opted to go straight to PR given the low-risk, test-only scope).

**One dangling thread, never concluded.** Mid-session, asked to research every Claude Code slash command related to code review (`/code-review`, `/code-review ultra`, `/security-review`, `/simplify` — differences, parameters, what each checks) for an unrelated side question. First pass came back from a background agent with several suspiciously precise, unverified-feeling claims (exact version gates, pricing figures, a `claude ultrareview` CLI subcommand) despite the task itself reporting `status: failed` (rate-limited). Sent it back asking it to explicitly confirm-or-flag-unconfirmed each specific claim rather than state them flatly. **Never followed up** — the owner's attention moved to the `setup/` audit and then the meetup demo, and the resumed agent's corrected report was never read or relayed. `ListAgents` at session end shows no reachable agent — it's gone, not paused. If this question resurfaces, it needs re-researching from scratch, not resumed.

### What was completed

- **PR #58 — MERGED.** M21.b's fold across all 4 packs + round-2 code-review fixes.
- **PR #59 — MERGED.** `file_is_spec_bearing` correction + `setup/` docs staleness fixes + round-3 code-review fixes (5 findings, 1 real bug).
- **Real branch protection enabled on `master`** (GitHub-enforced now, not just convention — see (b) above for the exact settings).
- **`codeowl-meetup-demo`** — built, pushed, demo delivered successfully. Done; revisit only if the owner raises it again.
- **PR #60 — OPEN, unmerged.** M21.c, all 4 items, zero production code changes (pure test-coverage work + one doc correction + one new open question).
- **New `ARCHITECTURE.md` open question 13** — method signatures and `interface_hash`, deliberately undecided.
- **`master`** is at `894bc2d` (PR #59's merge commit).

### Known issues / carry-forward

1. **PR #60 open, unmerged** — needs the owner's merge via the bypass-rules checkbox (branch protection is real now, so this is no longer optional theater — the merge button will actually refuse without it or the checkbox). Delete `m21c-cross-stack-test-gaps` only once `gh pr view 60` shows `MERGED`.
2. **The `/code-review`-commands research thread is genuinely unresolved** (see (d) above) — don't assume it was ever answered; re-research if it comes up again.
3. **The "CodeOwl Graph" visual-explainer Claude Artifact** (`https://claude.ai/artifact/15bTkfZxuKavwqHy4Vvd8a`, from a session before this transcript) has a stale `interface_hash` description (says the shipped fold is "source_hash-only" — no longer true post-M21.b). `setup/USAGE.md`'s equivalent FAQ answer *was* corrected this session; this artifact was flagged as having the same staleness but never actually fixed — the meetup demo detour took priority and it never got picked back up.
4. **`ARCHITECTURE.md` open questions 9, 10, and 11's remaining manifestations** (docstring inheritance, unoverridden members — both real, live in `commons-lang`) are still deliberately deferred, not blocking. **Open question 12 is resolved** (M21.b decided it, 2026-09-25: yes, fold — see `ARCHITECTURE.md` itself, already struck through) — don't re-list it as open. **New: open question 13** (method signatures in `interface_hash`, this session) joins 9/10/11 as deliberately undecided.
5. **The `deps.py` Python-2 syntax bug** (found back in M17) is still fixed only locally, never contributed upstream to `fastapi/full-stack-fastapi-template`.
6. **Gradle-based codegen is entirely unverified.** No Gradle-based Quarkus repo has been dogfooded.
7. **`.claude/` is untracked**, predates any of these sessions. Not mine, not a leftover — leave it.
8. **One dangling proposal, still never accepted or declined:** a FAQ entry distinguishing `get_callers` from `deps_hash`, drafted for `setup/USAGE.md` in an earlier session. Not added. Surface it again only if relevant.
9. **This session ran entirely on Sonnet 5** — no model-switch attribution drift this time (unlike the prior entry's item 5, now moot; the commit trailers below are accurate as written throughout).

### Exact next step to resume

**First: check PR #60** (`gh pr view 60`). If merged: `git checkout master && git pull`, delete `m21c-cross-stack-test-gaps` locally and on `origin` (only after confirming `MERGED`, per the standing rule), then `/mcp` reconnect is **not** needed — M21.c touched zero production code, only `tests/lifecycle_matrix.rs` and docs.

**Then: M22 — iterate the trait, ship the polyglot core.** `ROADMAP.md`'s M22 (`Size: M`, `Builds on: M21`) is the next real milestone: fold M17/M18's findings back into `trait StackPack`/`trait FeatureModel` (the leaks a non-Rust, non-web stack exposes that M15 couldn't have seen), finish M15's doc-neutralization, commit the Java corpora (commons-lang partial + quarkus-super-heroes feature sample). Two of its bullets already shipped early (`pending` pagination, same-package scan precision, both 2026-09-20) — read the section rather than assuming the whole list is outstanding.

**Separately, if it comes up again:** the `/code-review`-commands research question (carry-forward item 2) and the graph-explainer artifact's stale `interface_hash` line (item 3) both need picking back up from scratch — neither has a resumable agent or in-progress state to continue from.

### Git workflow (evergreen)

- **`master` is protected — GitHub-enforced since 2026-09-26, not just convention.** PR required, 1 approval, admins (Sidd) can bypass (`enforce_admins: false`), force-push and deletion both blocked. Every change → branch → PR → **Sidd merges** via the "Merge without waiting for requirements (bypass rules)" checkbox (he's the last pusher, so the 1-owner-approval gate can't be met otherwise — the intended path, now backed by an actual GitHub rule rather than just habit). Claude opens PRs (`gh pr create`) but **cannot merge** (`gh pr merge` is permission-blocked).
- Commits small, atomic, milestone-scoped where applicable (`M17: …`), each compiles + passes tests. Body carries `Authored by: Sidd & Claude Sonnet 5` plus the harness `Co-Authored-By:` / `Claude-Session:` trailers. PR body ends with the `🤖 Generated with Claude Code` line.
- `utility/check.sh` before every commit (fmt + clippy `-D warnings` + `cargo test` + staged-diff secret scan). `utility/release.sh` = same in `--release` + a release build, before a release or after perf/overflow-sensitive changes.
- **Delete a branch only after `gh pr view <n>` shows `MERGED`** (see Process notes).

## Hard invariants

- **CodeOwl never calls an LLM API and never holds LLM credentials.** It assembles context and persists results; the *calling agent* writes spec text via `get_next_spec_task` → write → `submit_spec`. If a task seems to need an LLM call from inside CodeOwl, the design is being violated — stop and flag it.
- **`get_spec` is a pure read.** It never triggers generation. Writes happen only via the `/codeowl generate` command. See ARCHITECTURE.md "Ordering".
- **Generation recurses across containment edges only, never reference edges.** See ARCHITECTURE.md "Recursive spec generation".
- **Invalidation never hashes LLM prose.** Reference edges key on `interfaceHash` (deterministic, off the graph). See ARCHITECTURE.md "Caching and invalidation".
- **The LLM never writes what the graph already knows.** Signatures come from extraction, dependency lists from resolved edges — CodeOwl fills those into a spec itself, and `spec_hash` covers only the LLM-written prose. If a generation prompt asks the agent to restate a signature, that's tokens spent on something we can't afford to have hallucinated. See ARCHITECTURE.md "Spec document format".

## Rust conventions

Written in Rust; see ARCHITECTURE.md "Implementation stack" for why and for the crate list.

- **Graph nodes reference each other by `SymbolId`, never by Rust references or `Rc<RefCell<…>>`.** Nodes live in a flat arena. This is both the idiomatic answer for a cyclic graph and what makes the `.codeowl/` cache cheap to serialize.
- **Don't let tree-sitter's `Node<'a>` escape the parse function.** Its lifetime is tied to its `Tree`, so storing one lifetime-infects everything downstream. Parse → walk → extract into owned structs → drop the tree.
- **Prefer owned `String` over borrowed `&str` in stored structs, and clone freely.** At laptop-repo scale the allocation cost is noise, and it avoids lifetime annotations spreading through the codebase. Reach for interning (`lasso`) only if profiling says string allocation is actually hot.
- **`anyhow::Result` with `.context(…)` for application errors.** Hand-rolled `thiserror` enums are for typed library contracts and are friction here.
- **Keep the core synchronous.** Extraction, resolution, hashing, and spec assembly are plain sync functions. Async appears only at the MCP transport (`rmcp` is tokio-based) and the file watcher.

This is a deliberate Rust learning project. When writing or reviewing Rust here, explain the idiom rather than just landing the fix — the reasoning is the point, not only the compiling code.

## Workflow

- **Commit attribution.** Git's `Author:` is Sidd (already the case). Add a human-readable `Authored by: Sidd & Claude Sonnet 5` line in the commit body — in addition to, never instead of, Claude Code's required `Co-Authored-By:`/`Claude-Session:` trailer, which is a fixed harness convention and stays on every commit regardless.
- **TDD is required for all major changes** — write the failing test first, implement to green. Concretely for this codebase: each `ROADMAP.md` milestone's stated validation *is* that test — write it (as an actual runnable test, not a manual check) before writing the code that satisfies it, not after. Skippable only for genuinely trivial changes (doc fixes, comment tweaks, a rename) — when in doubt, write the test first.
- **Never put secrets in a commit message or diff.** Scan staged content before every commit (`git diff --cached`, grep for key/token patterns), not only when something looks suspicious.
- **Commits are small, atomic, and milestone-scoped.** Reference the `ROADMAP.md` milestone a commit advances where applicable (e.g. `M3: add get_symbol tool`), and never leave a commit that doesn't compile or doesn't pass its tests (once there's code — see TDD above).
- **`.gitignore` from the first crate-scaffolding commit, not an afterthought.** `target/` and `.codeowl/` (the local gitignored cache — see ARCHITECTURE.md "Storage") must never land in git history, not even once.
- **`clippy` + `rustfmt` run before every commit, not just in CI.** Catches idiom mistakes early — matters more here given this is a Rust-learning project.
- **`utility/check.sh` bundles the whole gate** — `fmt --check` (or `--fix`), `clippy --all-targets -D warnings`, `cargo test`, and the staged-diff secret scan, in order, exiting on the first failure. Run it before every commit instead of the four commands separately. It's **debug-profile only**; `utility/release.sh` runs the same gate in `--release` plus `cargo build --release` (slower — for before a release or after touching perf/overflow-sensitive code, not every commit).
- **Stage first, then run the gate: `git add -A && utility/check.sh && git commit`.** The secret scan reads the *staged* diff, so running the gate before `git add` silently checks an empty index — fmt/clippy/tests still run against the working tree, but the scan is a no-op. It used to print a confident "nothing secret-shaped staged" for that case; it now refuses with a non-zero exit unless you pass `--unstaged`, which runs fmt/clippy/tests and prints `SKIPPED` for the scan. Use `--unstaged` for mid-work checks, never as the pre-commit path.

## Pending decisions

Deliberately unresolved, to revisit when implementation makes them concrete. Design-level open questions live in ARCHITECTURE.md's "Open questions" list (tree-sitter vs. LSP, the token-budget threshold, polyglot/non-code artifacts); these are the scope-trimming ones that don't belong there:

- **MCP tool surface — resolved by inaction, worth noting rather than reopening.** The shipped surface is 9 tools (`get_symbol`, `get_source`, `get_callers`, `get_callees`, `get_spec`, `get_next_spec_task`, `submit_spec`, `search_code`, `get_spec_coverage` — `get_source` shipped under the agent-reliance track's M20, between the session that first wrote this note and the one that corrected it) — `trace_path`, `get_tests_for`, `get_dependencies` were sketched but never built, simply because nothing has needed them yet; `impact_analysis` was never built either, but for a different reason — `get_callers` already answers "what breaks if I change this," so there was never a separate tool to build. See `ARCHITECTURE.md` §7 for the full current reference. Revisit only when something real calls for one of the three still-deferred tools.
- **Spec-regeneration commit hygiene.** Specs live in git, so a `/codeowl generate` run mid-feature drags spec diffs into an unrelated PR. Explicit generation (rather than silent) already makes this avoidable; the convention that makes it reliable — regenerate as its own commit, or as a deliberate pre-PR step — should be settled once there's a real workflow to test it against.

## Docs

Design decisions go in `ARCHITECTURE.md` / `REQUIREMENTS.md`, not in commit messages or code comments. When resolving an open question, update the open-questions list *and* the section it affects — leaving a resolved question listed as open is the drift this project exists to prevent.

When a doc or comment coins or leans on a domain term (static-analysis, graph-theory, or CodeOwl-specific), it belongs in `GLOSSARY.md` — keep that file in step as the vocabulary grows.

**Milestone numbers never appear in anything an agent reads.** `M17`, `M20`, "Phase 1" and friends are project bookkeeping — a calling agent can't act on them, they cost tokens in every session, and they silently rot the moment a milestone is renumbered. Keep them out of: `#[tool(description = …)]` strings, the `info.instructions` block, and **any `///` doc comment on a type deriving `JsonSchema`** — `schemars` turns those into the JSON Schema `description` shipped over the wire, so they're agent-facing even though they look like ordinary source comments. Describe the behaviour instead ("`pending` is paginated — 50 entries per call"), not its provenance. They stay welcome in `//` implementation and test comments, in `///` docs on internal types, and throughout `ROADMAP.md` / `ARCHITECTURE.md` / `GLOSSARY.md` / `experiments/` — those are read by humans and future sessions, where the provenance is the point. Same rule covers `setup/codeowl-generate.md` and its `.prompt.md` port: an agent executes those.
